// Background animations removed — OLED black from body { background: var(--black) }
// suffices. This stub keeps existing imports from breaking.
export default function Background() {
  return null;
}
